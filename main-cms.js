// Remove Empty CMS
if ( $('.cms-filter_item.series').length < 1 ) {
	$('.filter-contain.series').remove();
}



// Remove Empty CMS
if ( $('.cms-filter_item.set').length < 1 ) {
	$('.filter-contain.set').remove();
}



// Remove Empty CMS
if ( $('.cms-filter_item.product-type').length < 1 ) {
	$('.filter-contain.product-type').remove();
}



// Remove Empty CMS
if ( $('.cms-filter_item.card-type').length < 1 ) {
	$('.filter-contain.card-type').remove();
}


// Remove Empty CMS
if ( $('.cms-filter_item.card-color').length < 1 ) {
	$('.filter-contain.card-color').remove();
}



// Remove Empty CMS
if ( $('.cms-filter_item.rarity').length < 1 ) {
	$('.filter-contain.rarity').remove();
}



// Remove Empty CMS
if ( $('.cms-filter_item.card-finish').length < 1 ) {
	$('.filter-contain.card-finish').remove();
}
